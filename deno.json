{
  "version": "1.17.0",
  "tasks": {
    "commit": "deno task build && git add . && git commit",
    "build": "deno bundle src/main.ts > dist/slim.js && deno bundle --minify src/main.ts > dist/slim.min.js",
    "check": "deno check --unstable-raw-imports src/**/*.ts", 
    "fix": "deno task check && deno lint --fix src/**/*.ts && deno fmt src/**/*.ts",
    "server": "deno run --allow-all --watch --unstable-raw-imports src/server.ts"
  },
  "imports": {
    "@hono/hono": "jsr:@hono/hono@^4.9.2",
    "@std/assert": "jsr:@std/assert@1"
  },
  "compilerOptions": {
    "lib": ["dom", "ESNext", "DOM.Iterable", "deno.ns"]
  },
  "lint": {
    "rules": {
      "exclude": ["no-window"]
    }
  }
}
